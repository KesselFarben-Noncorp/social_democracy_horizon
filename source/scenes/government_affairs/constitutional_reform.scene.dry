title: Constitutional Reform
new-page: true
is-card: true
view-if: spd_in_government = 1 and justice_minister_party = "SPD" and constitutional_reform_timer = 0 and judicial_reform >= 4 and full_reform_initiated = 1 and constitutional_reform < 5 and ( coalition_votes >= 67 or (coalition_votes >= 50 and (kpd_in_government or sapd_in_government or ddp_in_government or z_in_government)) or (spd_r >= 40) or (dnvp_in_government = 0 and nsdap_r < 20 and coalition_votes >= 55) )
frequency: 25
on-arrival: {! Q.month_actions += 1; Q.constitutional_reform_timer += 12; Q.pass_threshold = 0.51; if (Q.pro_republic < 65) { Q.pass_threshold = 0.6; } !}
card-image: img/portraits/RadbruchGustav.jpg
go-to: reforms_menu
tags: govt_affairs, cabinet

# technically you could do some trickery to get a 2/3 quorum in the Reichstag by stopping some people from attending... if the NSDAP is boycotting the Reichstag, or if the NSDAP or DNVP are banned in the aftermath of a coup atempt...

# alternatively, a constitutional referendum can be initiated, that requires a simple majority of voters to pass (as I understand article 76). This should depend on support for the republic and for democracy, and the relationships with the other parties (more similar to the presidential election).

= Constitutional Reform

# the ideas for constitutional reform here are based on the bundesrepublik basic law... I'm not sure if it's totally realistic to what someone like Mierendorff would've wanted.

Many neorevisionists consider the Weimar Constitution to be inadequate. They believe that the clauses of the constitution are in part responsible for the instability of the country, and must change in order to build a more stable democratic republic. Ideas from both socialists and liberals

A constitutional reform requires either a two-thirds supermajority in the Reichstag, or a constitutional referendum with support from a majority of the electorate. If we do not have a supermajority in the Reichstag, we must be assured of strong support from the other parties for the constitutional referendum.

@reforms_menu

- @vote_threshold: Change the vote threshold.
- @alt_vote_threshold: Alterate the vote threshold.
- @constructive_vonc: Ban non-constructive votes of no confidence.
- @presidential_powers: Reduce presidential powers.
- @as_is: Do not pursue constitutional reform.
- @easy_discard
- @cancel_advisor_action

@alt_vote_threshold
view-if: electoral_threshold > 0
subtitle: Alterate the vote threshold.

We have come to the thought that alteration of the vote threshold is for the best. But we need to establish what value it should take. [+ electoral_threshold +]% is the current threshold for a party to hold seats in the Reichstag.

- @threshold_1: Propose a 1% threshold (minimal change).
- @threshold_3: Propose a 3% threshold (regional parties like the BVP remain viable).
- @threshold_5: Propose a 5% threshold (standard reform).
- @threshold_10: Propose a 10% threshold (only major parties survive).
- @reforms_menu: See other reform options.


@threshold_1
subtitle: Propose a 1% electoral threshold.
on-arrival: {!
    // Token threshold: draw each party name from Q.parties in turn and compare
    // its ideo_score, relation, and normalized weight to accumulate support.
    Q.reform_min_relation_base = 20;
    Q.reform_support = 0;
    for (var i = 0; i < Q.parties.length; i++) {
        Q.p = Q.parties[i];
        Q.ideo_diff = Math.abs(Q[Q.p + "_ideo"] - Q.spd_ideo);
        Q[Q.p + "_ideo_score"] = Q.ideo_diff / 19;
        Q.required_relation = Q.reform_min_relation_base * Q[Q.p + "_ideo_score"];
        if (Q[Q.p + "_relation"] >= Q.required_relation) {
            Q.reform_support += Q[Q.p + "_normalized"];
        }
    }
!}
view-if: electoral_threshold != 1

# Nearly everyone tolerates a token 1% threshold since it costs almost no one their seats - basically free support.

A 1% threshold is a token gesture, filtering out only the most marginal fringe lists. It is unlikely to face much organized opposition [? if electoral_threshold > 1 : , and would represent a significant loosening from the current [+ electoral_threshold +]% threshold ?].

- @threshold_1_2: Call a constitutional referendum to pass the reform!
- @alt_vote_threshold: Reconsider.


@threshold_1_2
choose-if: reform_support >= pass_threshold
unavailable-subtitle: We do not have enough support for this policy. [? if pro_republic < 65 : We should have an additional margin for the referendum since we need a majority of voters, and many voters who do not support the republic will not participate. ?]
on-arrival: {! Q.constitutional_reform += 1; Q.electoral_threshold = 1; !}

We have passed a constitutional amendment setting the electoral threshold to 1%. Minor and fringe parties retain their ability to enter the Reichstag.


@threshold_3
subtitle: Propose a 3% electoral threshold.
on-arrival: {!
    // Moderate threshold: draw each party name from Q.parties in turn and compare
    // its ideo_score, relation, and normalized weight to accumulate support.
    Q.reform_min_relation_base = 40;
    Q.reform_support = 0;
    for (var i = 0; i < Q.parties.length; i++) {
        Q.p = Q.parties[i];
        Q.ideo_diff = Math.abs(Q[Q.p + "_ideo"] - Q.spd_ideo);
        Q[Q.p + "_ideo_score"] = Q.ideo_diff / 19;
        Q.required_relation = Q.reform_min_relation_base * Q[Q.p + "_ideo_score"];
        if (Q[Q.p + "_relation"] >= Q.required_relation) {
            Q.reform_support += Q[Q.p + "_normalized"];
        }
    }
!}
view-if: electoral_threshold != 3

A 3% threshold would still allow regional parties like the BVP to survive in their strongholds, softening opposition considerably compared to a harsher cutoff. [? if z_relation > 20 : The Center Party is comfortable with this level. ?] [? if kpd_relation > 40 : The KPD is willing to tolerate this threshold. ?] [? if nsdap_normalized >= 0.15 : The NSDAP sees some advantage in a moderate threshold and lends cautious support. ?]

- @threshold_3_2: Call a constitutional referendum to pass the reform!
- @alt_vote_threshold: Reconsider.


@threshold_3_2
choose-if: reform_support >= pass_threshold
unavailable-subtitle: We do not have enough support for this policy. [? if pro_republic < 65 : We should have an additional margin for the referendum since we need a majority of voters, and many voters who do not support the republic will not participate. ?]
on-arrival: {! Q.constitutional_reform += 1; Q.electoral_threshold = 3; Q.left_dissent += 10; !}

We have passed a constitutional amendment setting the electoral threshold to 3%. Regional parties are likely to survive this change intact.


@threshold_5
subtitle: Propose a 5% electoral threshold.
on-arrival: {!
    // Higher threshold: draw each party name from Q.parties in turn and compare
    // its ideo_score, relation, and normalized weight to accumulate support.
    Q.reform_min_relation_base = 60;
    Q.reform_support = 0;
    for (var i = 0; i < Q.parties.length; i++) {
        Q.p = Q.parties[i];
        Q.ideo_diff = Math.abs(Q[Q.p + "_ideo"] - Q.spd_ideo);
        Q[Q.p + "_ideo_score"] = Q.ideo_diff / 19;
        Q.required_relation = Q.reform_min_relation_base * Q[Q.p + "_ideo_score"];
        if (Q[Q.p + "_relation"] >= Q.required_relation) {
            Q.reform_support += Q[Q.p + "_normalized"];
        }
    }
!}
view-if: electoral_threshold != 5

Some political thinkers believe that the proliferation of minor parties is responsible for the instability of the republic. They believe that a higher electoral threshold will reduce the influence of small radical parties. Parties that win a plurality in a constituency will maintain their representation, a gift to regional parties such as the BVP.

The smaller parties will obviously oppose this reform. [? if z_relation > 30 : The Center Party supports this reform, while their BVP partners oppose it. ?] [? if kpd_relation > 50 and kpd_normalized >= 0.1 : Perhaps surprisingly, the KPD supports this reform. ?] [? if nsdap_normalized >= 0.2 : The NSDAP supports this reform, as they believe they have something to gain. ?]

The SPD Left opposes this change, as they believe it to be anti-democratic.

- @threshold_5_2: Call a constitutional referendum to pass the reform!
- @alt_vote_threshold: Reconsider.


@threshold_5_2
choose-if: reform_support >= pass_threshold
unavailable-subtitle: We do not have enough support for this policy. [? if pro_republic < 65 : We should have an additional margin for the referendum since we need a majority of voters, and many voters who do not support the republic will not participate. ?] [? if z_relation <= 30 : The Center Party does not trust us enough to support this policy. ?]
on-arrival: {!
    Q.constitutional_reform += 1;
    Q.electoral_threshold = 5;
    Q.ddp_relation -= 25;
    Q.dvp_relation -= 25;
    Q.left_dissent += 20;
    Q.workers_dnvp += Q.workers_other * 0.4;
    Q.workers_nsdap += Q.workers_other * 0.4;
    Q.workers_other *= 0.1;
    Q.new_middle_dvp += Q.new_middle_other * 0.3;
    Q.new_middle_nsdap += Q.new_middle_other * 0.3;
    Q.new_middle_other *= 0.1;
    Q.old_middle_dvp += 0.3 * Q.old_middle_other;
    Q.old_middle_nsdap += 0.3 * Q.old_middle_other;
    Q.old_middle_dnvp += 0.3 * Q.old_middle_other;
    Q.old_middle_other *= 0.1;
    Q.rural_dnvp += 0.5 * Q.rural_other;
    Q.rural_nsdap += 0.4 * Q.rural_other;
    Q.rural_other *= 0.1;
    Q.unemployed_nsdap += 0.3 * Q.unemployed_other;
    Q.unemployed_kpd += 0.4 * Q.unemployed_other;
    Q.unemployed_other *= 0.2;
    Q.catholics_z += 0.6 * Q.catholics_other;
    Q.catholics_other *= 0.1;
!}

We have passed a constitutional amendment to increase the electoral threshold to 5%. This is likely to benefit the larger parties and harm the smaller ones.


@threshold_10
subtitle: Propose a 10% electoral threshold.
on-arrival: {!
    // Extreme threshold: draw each party name from Q.parties in turn and compare
    // its ideo_score, relation, and normalized weight to accumulate support.
    Q.reform_min_relation_base = 100;
    Q.reform_support = 0;
    for (var i = 0; i < Q.parties.length; i++) {
        Q.p = Q.parties[i];
        Q.ideo_diff = Math.abs(Q[Q.p + "_ideo"] - Q.spd_ideo);
        Q[Q.p + "_ideo_score"] = Q.ideo_diff / 19;
        Q.required_relation = Q.reform_min_relation_base * Q[Q.p + "_ideo_score"];
        if (Q[Q.p + "_relation"] >= Q.required_relation) {
            Q.reform_support += Q[Q.p + "_normalized"];
        }
    }
!}
view-if: electoral_threshold != 10

A 10% threshold is an extreme measure that would wipe out nearly every minor party overnight. Almost no one supports this beyond the largest parties, and even they are wary of setting a precedent that could later be turned against them.

- @threshold_10_2: Call a constitutional referendum to pass the reform!
- @alt_vote_threshold: Reconsider.


@threshold_10_2
choose-if: reform_support >= pass_threshold
unavailable-subtitle: We do not have enough support for this policy - a 10% threshold is broadly unpopular, even among our own coalition.
on-arrival: {! Q.constitutional_reform += 1; Q.electoral_threshold = 10; Q.ddp_relation -= 40; Q.dvp_relation -= 40; Q.left_dissent += 35; Q.z_relation -= 15; !}

We have passed a radical constitutional amendment setting the electoral threshold to 10%. Minor and regional parties are effectively eliminated from the Reichstag.

@vote_threshold
subtitle: Change the vote threshold to 5% for parties to be represented in the Reichstag.
on-arrival: {!
    // Same as threshold_5: draw each party name from Q.parties in turn and compare
    // its ideo_score, relation, and normalized weight to accumulate support.
    Q.reform_min_relation_base = 60;
    Q.reform_support = 0;
    for (var i = 0; i < Q.parties.length; i++) {
        Q.p = Q.parties[i];
        Q.ideo_diff = Math.abs(Q[Q.p + "_ideo"] - Q.spd_ideo);
        Q[Q.p + "_ideo_score"] = Q.ideo_diff / 19;
        Q.required_relation = Q.reform_min_relation_base * Q[Q.p + "_ideo_score"];
        if (Q[Q.p + "_relation"] >= Q.required_relation) {
            Q.reform_support += Q[Q.p + "_normalized"];
        }
    }
!}
view-if: electoral_threshold = 0

# The left is opposed to this.

Some political thinkers believe that the proliferation of minor parties is responsible for the instability of the republic. They believe that a higher electoral threshold will reduce the influence of small radical parties. Parties that win a plurality in a constituency will maintain their representation, a gift to regional parties such as the BVP.

The smaller parties will obviously oppose this reform. [? if z_relation > 30 : The Center Party supports this reform, while their BVP partners oppose it. ?] [? if kpd_relation > 50 and kpd_normalized >= 0.1 : Perhaps surprisingly, the KPD supports this reform. ?] [? if nsdap_normalized >= 0.2 : The NSDAP supports this reform, as they believe they have something to gain. ?]

The SPD Left opposes this change, as they believe it to be anti-democratic.

- @vote_threshold_2: Call a constitutional referendum to pass the reform!
- @reforms_menu: See other reform options.


@vote_threshold_2
choose-if: reform_support >= pass_threshold
unavailable-subtitle: We do not have enough support for this policy. [? if pro_republic < 65 : We should have an additional margin for the referendum since we need a majority of voters, and many voters who do not support the republic will not participate. ?] [? if z_relation <= 30 : The Center Party does not trust us enough to support this policy. ?]
on-arrival: {!
    Q.constitutional_reform += 1;
    Q.electoral_threshold = 5;
    Q.ddp_relation -= 25;
    Q.dvp_relation -= 25;
    Q.left_dissent += 20;
    Q.workers_dnvp += Q.workers_other * 0.4;
    Q.workers_nsdap += Q.workers_other * 0.4;
    Q.workers_other *= 0.1;
    Q.new_middle_dvp += Q.new_middle_other * 0.3;
    Q.new_middle_nsdap += Q.new_middle_other * 0.3;
    Q.new_middle_other *= 0.1;
    Q.old_middle_dvp += 0.3 * Q.old_middle_other;
    Q.old_middle_nsdap += 0.3 * Q.old_middle_other;
    Q.old_middle_dnvp += 0.3 * Q.old_middle_other;
    Q.old_middle_other *= 0.1;
    Q.rural_dnvp += 0.5 * Q.rural_other;
    Q.rural_nsdap += 0.4 * Q.rural_other;
    Q.rural_other *= 0.1;
    Q.unemployed_nsdap += 0.3 * Q.unemployed_other;
    Q.unemployed_kpd += 0.4 * Q.unemployed_other;
    Q.unemployed_other *= 0.2;
    Q.catholics_z += 0.6 * Q.catholics_other;
    Q.catholics_other *= 0.1;
!}

# this should result in a redistribution of votes - other parties should not get so many votes anymore. maybe ddp and dvp get a recovery of middle-class votes? or maybe they flow to the others.

We have passed a constitutional amendment to increase the electoral threshold to 5%. This is likely to benefit the larger parties and harm the smaller ones.


@constructive_vonc
subtitle: Votes of no confidence can only be called if an alternative plan for government formation can be passed, greatly reducing their incidence.
on-arrival: {!
    // Governing-stability reform: draw each party name from Q.parties in turn and
    // compare its ideo_score, relation, and normalized weight to accumulate support.
    Q.reform_min_relation_base = 50;
    Q.reform_support = 0;
    for (var i = 0; i < Q.parties.length; i++) {
        Q.p = Q.parties[i];
        Q.ideo_diff = Math.abs(Q[Q.p + "_ideo"] - Q.spd_ideo);
        Q[Q.p + "_ideo_score"] = Q.ideo_diff / 19;
        Q.required_relation = Q.reform_min_relation_base * Q[Q.p + "_ideo_score"];
        if (Q[Q.p + "_relation"] >= Q.required_relation) {
            Q.reform_support += Q[Q.p + "_normalized"];
        }
    }
!}
view-if: constructive_vonc = 0

[? if z_relation >= 30 : The Weimar Coalition parties, the [+ ddp_name +] and Center Party, support this reform, which will greatly improve the stability of the government. ?] [? if z_relation < 30 : The [+ ddp_name +] supports this reform, but the Center Party does not trust us enough to support the constitutional referendum. ?] The more conservative and radical right-wing parties and the KPD are in opposition, as they seem to relish in the republican system's instability.

- @constructive_vonc_2: Call a constitutional referendum to pass the reform!
- @reforms_menu: See other reform options.

@constructive_vonc_2
choose-if: reform_support >= pass_threshold
unavailable-subtitle: We do not have enough support for this policy. [? if pro_republic < 65 : We should have an additional margin for the referendum since we need a majority of voters, and many voters who do not support the republic will not participate. ?] [? if z_relation <= 30 : The Center Party does not trust us enough to support this policy. ?]
on-arrival: {! Q.constitutional_reform += 1; Q.constructive_vonc = 1; !}

We have successfully passed a constitutional amendment to ban votes of no confidence when a new government cannot be formed. Hopefully this increases the stability of our governments.

@presidential_powers
subtitle: The president can no longer dissolve the Reichstag at will, or call a *Reichsexekution* on state governments.
on-arrival: {!

    Q.reform_min_relation_base = 50;
    Q.reform_support = 0;
    for (var i = 0; i < Q.parties.length; i++) {
        Q.p = Q.parties[i];
        Q.ideo_diff = Math.abs(Q[Q.p + "_ideo"] - Q.spd_ideo);
        Q[Q.p + "_ideo_score"] = Q.ideo_diff / 19;
        Q.required_relation = Q.reform_min_relation_base * Q[Q.p + "_ideo_score"];
        if (Q[Q.p + "_relation"] >= Q.required_relation) {
            Q.reform_support += Q[Q.p + "_normalized"];
        }
    }
!}
view-if: presidential_powers = 0

The wide-ranging presidential powers promised by the constitution are a potential gateway to authoritarianism. Thus, we can support reforms that limit the powers of the president, ending his power to unilaterally terminate an elected government and to remove state governments.

The KPD is in favor of this reform, having been victims of the *Reichsexekution* in the past (when KPD-supported state governments in Thuringia and Saxony were removed). The democrats of the [+ ddp_name +] also support this. [? if president == "Hindenburg": However, the opposition from conservatives is vehement, as they see a reduction in presidential powers as disrespectful to Hindenburg. ?] [? if z_relation <= 49 and president == "Hindenburg" : Thus, the Center Party has been influenced to oppose these reforms. ?] [? if z_relation > 49 and president == "Hindenburg" : Despite this, the Center Party supports the proposed reform. ?] [? if president == "Braun" : The Center Party supports this reform. ?]

- @presidential_powers_2: Call a constitutional referendum to pass the reform!
- @reforms_menu: See other reform options.

@presidential_powers_2
choose-if: reform_support >= pass_threshold
unavailable-subtitle: We do not have enough support for this policy. [? if pro_republic < 65 : We should have an additional margin for the referendum since we need a majority of voters, and many voters who do not support the republic will not participate. ?] [? if z_relation <= 49 and president != "Braun" : The Center Party does not trust us enough to support this policy. ?]
on-arrival: {! Q.constitutional_reform += 1; Q.presidential_powers = 1; !}

We have successfully passed a constitutional amendment to limit presidential powers! Hopefully this results in a less authoritarian Germany.

@as_is
go-to: root
