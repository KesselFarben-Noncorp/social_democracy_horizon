# Simple workflow for deploying static content to GitHub Pages
name: Deploy static content to Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: 24  # Changed from 16 to 24
          cache: 'npm'      # Added caching to speed up your "npm install" step

      - name: Install
        run: npm ci        # Using 'npm ci' is faster and more reliable for CI/CD

      - name: Build game
        run: npm run dendrynexus make-html -- --pretty

      - name: Copy game.json to output
        run: cp out/game.json out/html/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: 'out/html'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
